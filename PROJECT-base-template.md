# PROJECT.md
# Project-specific configuration for Claude Code.
# Fill in this file when starting a new project.
# This file is read by Claude Code at the start of every session (via CLAUDE.md).
# Do not leave sections blank — write "none" or "default" if not applicable.

---

## Project Identity

- **Name:** 
- **Type:**                    # e.g. macOS app, CLI tool, REST API, Python library, web app
- **One-liner:**               # what this project does in one sentence
- **Target platform:**         # e.g. macOS 14+, Linux, cross-platform, browser
- **Target users:**            # e.g. developers, end users, internal team
- **Distribution:**            # e.g. PyPI, npm, GitHub Releases, direct download, internal, none

---

## Design References

# URLs or descriptions of reference projects, apps, or design examples.
# Claude Code will extract patterns and ideas — never copy code or assets.
- 

---

## Git Conventions

- **Commit style:**            # e.g. Conventional Commits (feat/fix/chore), free-form, none
- **Branch strategy:**         # e.g. main only, feature branches, gitflow
- **PR required:**             # yes / no
- **Protected branches:**      # e.g. main, release — Claude Code must never force-push these

---

## Quality Gates

- **Tests must pass before:**  # e.g. every commit, every release, never
- **Code review required:**    # yes (Claude Code reviews its own output) / no
- **Minimum test coverage:**   # e.g. 80%, none defined

---

## Claude Code Preferences

- **Ask before adding a dependency:**      yes / no
- **Ask before deleting any file:**        yes / no
- **Ask before modifying design docs:**    yes / no
- **Ask before running release steps:**    yes / no
- **Preferred response style:**            # e.g. concise, detailed, bullet points
- **Language for all docs:**               # e.g. English

---

## Out of Scope

# List anything Claude Code must never do in this project.
# Examples:
# - Never touch the legacy/ folder
# - Never modify the CI/CD pipeline files
# - Never change database schema without explicit instruction
#
- 

---

## Notes

# Any other context Claude Code should know about this project.
# Examples: known constraints, external dependencies, team agreements, links to external docs.
#